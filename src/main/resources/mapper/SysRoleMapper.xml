<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aaa.system.mapper.SysRoleMapper">

<!--    <resultMap id="RoleMap" type="com..model.system.SysRole" autoMapping="true">-->
<!--    </resultMap>-->

<!--    &lt;!&ndash; 用于select查询公用抽取的列 &ndash;&gt;-->
<!--    <sql id="columns">-->
<!--        id,role_name,role_code,description,create_time,update_time,is_deleted-->
<!--    </sql>-->
    <resultMap id="RoleMap" type="com.aaa.model.system.SysRole" autoMapping="true">

    </resultMap>

    <select id="selectPage" resultMap="RoleMap">
        select id,role_name,role_code,description,create_time,update_time,is_deleted
        from sys_role
        <where>
            <if test="vo.roleName != null and vo.roleName != ''">
                and role_name like CONCAT('%',#{vo.roleName},'%')
            </if>
            and is_deleted = 0
        </where>
        order by create_time desc
    </select>

</mapper>
